version: '3.8'

services:

  db:
    image: postgres:12.3
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: blog_admin
      POSTGRES_PASSWORD: something
      POSTGRES_DB: blog
    volumes:
      - ./.data/pgdata:/var/lib/postgresql/data
